"""The split-mode investment council, rebuilt in Python (owner ruling AB8).

Everything that runs a council lives in this package: the evidence gate,
freeze and sufficiency check; the host that drives the seats; the challenger
bridge; the publisher, read-back and report renderer. Standard library only.
Scope is docs/REBUILD-SPEC.md, frozen; owner rulings override everything.
"""
